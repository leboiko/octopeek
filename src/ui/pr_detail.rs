//! PR / issue detail panel.
#![allow(dead_code)]
//!
//! Phase 3: renders the full body (markdown rendered via pulldown-cmark),
//! review timeline, comments, and action buttons.

use crate::app::App;
use ratatui::{Frame, layout::Rect};

/// Render the PR / issue detail view (stub — Phase 3).
pub fn draw(_f: &mut Frame, _app: &App, _area: Rect) {
    // Phase 3: render PR body, review timeline, comments.
}
