//! Repo picker overlay for adding or removing watched repositories.
#![allow(dead_code)]
//!
//! Phase 3: renders a fuzzy-searchable list of repos from the GitHub API,
//! letting the user pin or unpin repos to the tab bar.

use crate::app::App;
use ratatui::{Frame, layout::Rect};

/// Render the repo picker overlay (stub — Phase 3).
pub fn draw(_f: &mut Frame, _app: &App, _area: Rect) {
    // Phase 3: render searchable repo list with pin/unpin actions.
}
