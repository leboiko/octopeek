//! Dashboard panel: the main PR / issue inbox list.
#![allow(dead_code)]
//!
//! Phase 3: implements the scrollable list with role glyphs (A/R/@),
//! status indicators, and selection cursor.

use crate::app::App;
use ratatui::{Frame, layout::Rect};

/// Render the dashboard panel (stub — Phase 3).
pub fn draw(_f: &mut Frame, _app: &App, _area: Rect) {
    // Phase 3: render the PR / issue list with role glyphs and selection.
}
